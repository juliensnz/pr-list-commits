name: "PR List commits"
description: "Sets the PR List commits for other steps"
inputs:
  repo-token:
    description: "GITHUB_TOKEN secret"
    required: true
runs:
  using: "node12"
  main: "dist/index.js"
branding:
  icon: "git-branch"
  color: "gray-dark"
